// Contenu de renderer.js
function enregistrerMotDePasse() {
    const site = document.getElementById('site').value;
    const utilisateur = document.getElementById('utilisateur').value;
    const motDePasse = document.getElementById('mot_de_passe').value;

    // Ajoutez la logique pour enregistrer le mot de passe
    console.log(`Enregistrement - Site: ${site}, Utilisateur: ${utilisateur}, Mot de passe: ${motDePasse}`);
}

function afficherMotsDePasse() {
    // Ajoutez la logique pour afficher les mots de passe
    console.log('Affichage des mots de passe');
}